import { CountryDirectiveDirective } from './country-directive.directive';

describe('CountryDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
